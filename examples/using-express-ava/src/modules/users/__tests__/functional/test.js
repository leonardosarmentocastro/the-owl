const test = require('ava');

test('returns the given user if it exists', t => {
  console.log('text name:', t.title);
  t.pass();
});

test('returns an error if the given user doesnt exist', t => {

});
